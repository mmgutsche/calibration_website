# pytest.ini
[pytest]
addopts = --strict-markers
markers =
    asyncio: marks tests as asyncio (select with '-m asyncio')

asyncio_mode = auto
testpaths =
    tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
